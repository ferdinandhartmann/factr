--- a/factr/models/lowdim_action_transformer.py
+++ b/factr/models/lowdim_action_transformer.py
@@ -357,6 +357,9 @@
         **kwargs,
     ):
         del imgs
+        sample_temperature = float(kwargs.get("sample_temperature", 1.0))
+        if sample_temperature <= 0:
+            raise ValueError(f"sample_temperature must be > 0, got {sample_temperature}.")
         context_tokens = self._build_context_tokens(obs, class_labels=class_labels)
         z_context = self._build_z_context(context_tokens)
         mu_p, logvar_p = self._prior(z_context)
@@ -364,7 +367,7 @@
         batch_size, z_dim = mu_p.shape
         if sample:
             eps = torch.randn(batch_size, num_samples, z_dim, device=mu_p.device)
-            z = mu_p.unsqueeze(1) + torch.exp(0.5 * logvar_p).unsqueeze(1) * eps
+            z = mu_p.unsqueeze(1) + sample_temperature * torch.exp(0.5 * logvar_p).unsqueeze(1) * eps
         else:
             z = mu_p.unsqueeze(1).expand(-1, num_samples, -1)
 
@@ -380,6 +383,9 @@
     @torch.no_grad()
     def get_actions_pos(self, imgs, obs, target_action, class_labels=None, num_samples=1, sample=True, **kwargs):
         del imgs
+        sample_temperature = float(kwargs.get("sample_temperature", 1.0))
+        if sample_temperature <= 0:
+            raise ValueError(f"sample_temperature must be > 0, got {sample_temperature}.")
         target_action = self._reshape_actions(target_action)
         context_tokens = self._build_context_tokens(obs, class_labels=class_labels)
 
@@ -387,7 +393,7 @@
         batch_size, z_dim = mu_q.shape
         if sample:
             eps = torch.randn(batch_size, num_samples, z_dim, device=mu_q.device)
-            z = mu_q.unsqueeze(1) + torch.exp(0.5 * logvar_q).unsqueeze(1) * eps
+            z = mu_q.unsqueeze(1) + sample_temperature * torch.exp(0.5 * logvar_q).unsqueeze(1) * eps
         else:
             z = mu_q.unsqueeze(1).expand(-1, num_samples, -1)
 
--- a/factr/task.py
+++ b/factr/task.py
@@ -216,12 +216,14 @@
         eval_plot_max_steps: int = 300,
         eval_plot_prediction_stride: int = 5,
         eval_plot_num_samples: int = 10,
+        eval_plot_sample_temperature: float = 1.0,
     ):
         self.n_cams, self.obs_dim, self.ac_dim = n_cams, obs_dim, ac_dim
         self.train_loader = _build_data_loader(train_buffer, batch_size, num_workers, is_train=True)
         self.eval_plot_max_steps = int(eval_plot_max_steps)
         self.eval_plot_prediction_stride = max(1, int(eval_plot_prediction_stride))
         self.eval_plot_num_samples = max(1, int(eval_plot_num_samples))
+        self.eval_plot_sample_temperature = float(eval_plot_sample_temperature)
 
         self.weights_history = []
         self.weights_steps = []
@@ -266,7 +268,7 @@
         return pred_actions
 
     @staticmethod
-    def _sample_actions_for_plot(model, imgs, obs, labels, num_samples):
+    def _sample_actions_for_plot(model, imgs, obs, labels, num_samples, sample_temperature):
         try:
             pred_actions = model.get_actions_prior(
                 imgs,
@@ -274,6 +276,7 @@
                 class_labels=labels,
                 sample=True,
                 num_samples=num_samples,
+                sample_temperature=sample_temperature,
             )
         except TypeError:
             pred_actions = model.get_actions_prior(
@@ -281,6 +284,7 @@
                 obs,
                 sample=True,
                 num_samples=num_samples,
+                sample_temperature=sample_temperature,
             )
 
         if pred_actions.ndim == 3:
@@ -302,6 +306,7 @@
         first_plot_sample = None
         plot_samples = {"obs": [], "actions": [], "mask": [], "labels": [], "imgs": {}, "time_index": []}
         raw_eval_index = 0
+        prior_sample_action_std_mean = float("nan")
 
         model = trainer.model.module if hasattr(trainer.model, "module") else trainer.model
         was_training = model.training
@@ -407,7 +412,7 @@
             f"prior_std: {mean_prior_std:.4f}\tpost_std: {mean_posterior_std:.4f}\t"
             f"prior_H: {mean_prior_entropy:.4f}\tpost_H: {mean_posterior_entropy:.4f}\t"
             f"plot_steps: {len(plot_samples['obs'])}\tplot_stride: {self.eval_plot_prediction_stride}\t"
-            f"plot_samples: {self.eval_plot_num_samples}"
+            f"plot_samples: {self.eval_plot_num_samples}\tplot_temp: {self.eval_plot_sample_temperature:.2f}"
         )
 
         if wandb.run is not None:
@@ -465,12 +470,17 @@
                     obs=plot_obs,
                     labels=plot_labels,
                     num_samples=self.eval_plot_num_samples,
+                    sample_temperature=self.eval_plot_sample_temperature,
                 )
                 sampled_actions = sampled_actions.detach().cpu().numpy()
                 true_action_chunks = plot_actions.detach().cpu().numpy()
                 mask_chunks = plot_mask.detach().cpu().numpy()
                 label_arr = plot_labels.detach().cpu().numpy().reshape(-1)
 
+                sample_std = np.std(sampled_actions, axis=1)
+                denom = np.clip(mask_chunks.sum(), 1e-8, None)
+                prior_sample_action_std_mean = float((sample_std * mask_chunks).sum() / denom)
+
                 fig_fan_all = _build_eval_trajectory_fan_figure(
                     true_action_chunks=true_action_chunks,
                     pred_action_chunks=sampled_actions,
@@ -498,3 +508,16 @@
                     if fig_stiff is not None:
                         wandb.log({f"eval/prior_fan_stiffness_{int(stiffness_label)}": wandb.Image(fig_stiff)}, step=global_step)
                         plt.close(fig_stiff)
+
+                wandb.log(
+                    {
+                        "eval/prior_sample_action_std_mean": prior_sample_action_std_mean,
+                        "eval/prior_sample_temperature": float(self.eval_plot_sample_temperature),
+                    },
+                    step=global_step,
+                )
+
+        print(
+            f"Step: {global_step}\tSample diversity (mean std over sampled prior actions): "
+            f"{prior_sample_action_std_mean:.6f}"
+        )
--- a/factr/train_bc_policy.py
+++ b/factr/train_bc_policy.py
@@ -103,7 +103,8 @@
                 "Eval plot config | "
                 f"max_steps={task.eval_plot_max_steps} "
                 f"stride={task.eval_plot_prediction_stride} "
-                f"num_samples={task.eval_plot_num_samples}"
+                f"num_samples={task.eval_plot_num_samples} "
+                f"sample_temperature={getattr(task, 'eval_plot_sample_temperature', 1.0)}"
             )
 
         # create a gpu train transform (if used)
--- a/factr/cfg/task/single_franka_lowdim.yaml
+++ b/factr/cfg/task/single_franka_lowdim.yaml
@@ -6,6 +6,7 @@
 eval_plot_max_steps: 300
 eval_plot_prediction_stride: 5
 eval_plot_num_samples: 10
+eval_plot_sample_temperature: 1.5
 
 train_buffer:
   _target_: factr.replay_buffer.RobobufReplayBufferLowdim
--- a/LOWDIM_TRAINING_DIAGNOSTICS_GUIDE.md
+++ b/LOWDIM_TRAINING_DIAGNOSTICS_GUIDE.md
@@ -46,6 +46,7 @@
 - `eval_plot_max_steps: 300`
 - `eval_plot_prediction_stride: 5`
 - `eval_plot_num_samples: 10`
+- `eval_plot_sample_temperature: 1.5`
 
 ---
 
@@ -143,6 +144,9 @@
 - `eval_plot_max_steps`, `eval_plot_prediction_stride`, `eval_plot_num_samples`:
   - Control coverage/detail of fan plots.
   - Larger values improve diagnostics but increase eval runtime.
+- `eval_plot_sample_temperature`:
+  - Multiplies latent sampling noise during fan plotting/inference sampling.
+  - Larger values increase trajectory spread; too large can make samples unrealistic.
 
 ## Model params (`transformer_lowdim.yaml`)
 - `d_z`:
